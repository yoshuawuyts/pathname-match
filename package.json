{
  "name": "pathname-match",
  "version": "1.1.2",
  "description": "Strip a url to only match the `pathname`.",
  "main": "index.js",
  "scripts": {
    "test": "standard && NODE_ENV=test node test",
    "test-cov": "NODE_ENV=test istanbul cover test.js"
  },
  "repository": "yoshuawuyts/pathname-match",
  "keywords": [
    "path",
    "match",
    "strip",
    "wayfarer"
  ],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "istanbul": "^0.3.5",
    "standard": "^5.0.0",
    "tape": "^3.0.3"
  },
  "files": [
    "LICENSE",
    "index.js",
    "README.md"
  ]
}
